{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Backend for phonebook app",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../frontend && npm install && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add dist && git commit -m \"phonebook uibuild\" && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
